# Knowledge Hub × design-agent-ts｜项目拷打题库

> **写法对齐** `个人项目/interview`：  
> - 答题结构：`Qn` → **【答案】**（本质 / 分层 / 取舍 / 业界对照 / 本项目落地）→ **【补充】**（收口、诚实边界、反模式）→ `Qn-k` 子追问深挖。  
> - 贯穿原则（同源）：上下文稀缺 · 可验证信号驱动 · **fail loud 非 fail silent** · 恢复的是状态不是过程 · **设计时智能 vs 运行时智能**。  
> - 覆盖面映射 `interview/agent-llm` 知识地图（01–15），再加 **产品岗** 专章；不是「功能清单背诵」，是「领域怎么做 + 你项目怎么落地」。  
>
> 仓库：https://github.com/WangHNoob/knowledge-hub · https://github.com/WangHNoob/design-agent-ts

---

## 使用说明

| 读者 | 建议路径 |
|------|----------|
| 技术岗 | 第一部 01–15 全刷；重点 01/04/06/08/09/11/14 |
| 产品岗 | 第二部全刷 + 第一部 09/11/06 的「产品可读」答法 |
| 综合面 | 第三部交叉题 + 任选一套模拟套餐 |

面试口述时把【答案】压到 40%：先讲本质一句，再给 2 个项目证据，最后主动抛诚实边界。

---

# 第一部｜技术岗（对齐 agent-llm 覆盖面）

---

## 01 · 框架架构与解耦

### Q1：design-agent 为什么要 port / adapter / core 三层？和「直接写 LangGraph」比，本质命题是什么？

**【答案】**

本质命题与 interview 库一致：**业务积累不绑定任何 SDK**。AI 编排框架季度级迭代，若 Director、冲突检测、HITL、MCP 钩子直接 pen 进 LangGraph API，换栈等于推倒。

三层对应 Ports & Adapters：

- **port**：工具、MCP、模型、HITL、队列等能力的接口（稳定契约）。  
- **core**：Director/子 Agent/流水线/Plan 硬保障/冲突检测——**零框架依赖**的领域资产。  
- **adapter**：LangGraph、Postgres、MCP SDK、Redis……易变边缘。

依赖方向永远 `adapter → core → port`。换运行时 = 新写 adapter + 改 bootstrap 组装；core 里的策划协作语义应零改或极少改。

和「建在 SDK 之上而非对抗」一致：推理循环、图执行可复用 LangGraph；你抽象的是「业务不该关心的差异」（持久化会话、工具风险、MCP 暴露策略），不是重造 LLM 调用。

**【补充】**

- 一句话收口：护城河不是「用过 LangGraph」，是 **Trace/评测/工具契约/人机策略能独立于编排引擎演进**。  
- 诚实边界：抽象有前期成本；若永远只接一个引擎且团队只有一人，ROI 变低——但你已接 MCP/PG/Redis 等多适配，解耦账算得过来。  
- 反模式：为好看拆 20 个 port 却在 core 里 `import '@langchain/...'`——解耦名存实亡。

### Q1-1：Knowledge Hub 的 Route → Service → DB 和 Agent 侧三层是同一思想吗？

**【答案】**

同一思想、不同落点：

- KH：**确定性写路径**必须可审计——发布、纠正、别名写入走 Service；事件订阅者只编排触发，禁止旁路改 published OKF。  
- Agent：**不确定运行时智能**关在编排层；领域策略（何时 report、何时 HITL）留在 core/hook。

两边都在回答：哪些是可替换的「引擎」，哪些是必须捏在手里的「带业务语义的资产」。

**【补充】**

- KH 换 Fastify→其他 HTTP 框架应不伤 release/flywheel；Agent 换 LangGraph→其他图引擎应不伤冲突检测与 MCP 回写契约。

### Q1-2：换 SDK「真零改动」吗？你会怎么量化迁移成本？

**【答案】**

诚实说：**接近零改 core，不是零改仓库**。迁移成本 = 新 adaptor 行数 + 组装点 + 回归套件（HITL/工具熔断/MCP 钩子）。interview 实践里用「双 adaptor 共存共测」验证可切换；你这边的验收标准应是：同一套冲突检测与 `kb_report_*` 钩子在新引擎上绿。

**【补充】**

- 不要吹「一天换完」；吹「资产没被引擎绑架，映射是工程问题不是重学问题」。

---

## 02 · 可观测性

### Q2：Agent / 知识链路的可观测，和普通微服务 Trace 差在哪？

**【答案】**

微服务 Trace 关心 RPC 延迟与错误码。Agent/知识链路还必须回答业务语义：

- 这次回答依据的是哪个 **releaseId**？命中哪些 **componentIds / evidenceIds**？  
- 工具调用是读还是写？是否触发飞轮事件？  
- 构建 run 卡在哪一阶段？自动发布跳过的 **reason** 是什么？

因此观测数据应是**可信快照**：KH 用 `x-trace-id` + diagnostic span（可落库）；Agent 侧 Session/Trace/Span 写一次不宜乱改。观测是评测与事故复盘的原料，不是日志美化。

**【补充】**

- 业界对照：OTel 语义约定 + 业务属性扩展；LangSmith 类产品强在 Agent 语义，企业常要收回自有权限体系——KH 把诊断留在自有库是同一动机。  
- 诚实边界：你还没接完整 OTel Collector 导出；有 traceId 贯通与诊断页，网关级指标需演进。

### Q2-1：「恢复的是状态不是过程」如何体现在构建日志 SSE / Agent 重连？

**【答案】**

构建进度 SSE 推的是帧，断线后客户端应能根据 **runId + DB 中 run 状态机**（current_stage/completed_stages）恢复「做到哪了」，而不是要求服务端重放每一个字节。Agent 长任务同理：持久化执行状态后，重连订阅的是任务态，不是历史 token 流本身。

**【补充】**

- 反模式：只把日志打到 stdout，进程重启后前端无限转圈且无法 resume。

---

## 03 · 评测

### Q3：不确定输出如何「可度量可复现」？你项目里评测分层是什么？

**【答案】**

按 interview 评测六要素思维：dataset / case / metric / baseline / task / score 解耦。

落到本项目：

| 层 | 现状 | 对应问题 |
|----|------|----------|
| 检索离线 | `eval:retrieval` + 黄金集 hit@k | Context Recall 方向 |
| 知识门禁 | trust / lint / 治理 Profile | 「能不能发布」 |
| Agent 运行时 | 工具循环护栏、冲突检测、归因完整率 | 「做对了没有」 |
| 线上回流 | agent_events → 反馈簇 → 例外 | online 信号 → 离线改进 |

原则：**可验证信号驱动**，不用模型自评「我很准确」。LLM-as-Judge 若上，要去偏（位置交换等），且 ideally 与生成模型分离。

**【补充】**

- 诚实边界：尚无完整 RAGAS 四维 CI、无多 Judge 门禁；当前是 Answer Readiness **最小集**。  
- 一句话：没有评测的飞轮是自动驾驶关掉仪表盘。

### Q3-1：hit@k 涨了但用户说 Agent 更蠢，评测体系哪里盲？

**【答案】**

命中页 ≠ Faithfulness ≠ Answer Relevancy。可能：过期 release、双源污染、错工具（该 `kb_query_table` 却语义搜）、低 trust 仍被生成层无视、冲突字段未结构化。应用会话回放 + 归因面板补「线上 case → 离线集」，而不是只刷检索分。

---

## 04 · 工具失败与弹性

### Q4：工具失败为什么要「逐级升级」？Agent 侧你怎么做的？

**【答案】**

工具失败若一律抛给用户或一律重试，会抖或静默错。最佳实践是分层决策：重试（瞬时）→ 换路/降级 → 熔断隔舱 → 升级人工/明确失败（fail loud）。

design-agent：外部/MCP 工具 `ResilientToolWrapper`（超时、重试、耗尽后 return_to_llm）、`ToolCircuitRegistry` 熔断；`kb_*` 进读风险前缀，减少无意义 HITL。KH：`publish` 跳过带 reason；别名修复无动作则 `no_action`，避免空转重试风暴。

**【补充】**

- 业界：舱壁隔离、超时预算、幂等写。  
- 反模式：捕获异常后返回「好的」空字符串——典型 fail silent。

### Q4-1：MCP 超时与 KH 构建失败，弹性策略为何不同？

**【答案】**

MCP 读：可重试、可熔断、可降级到「明确无结果」。  
构建/发布写：失败要落 run 状态与例外，**不能**自动无限重试 full build（成本与抖动）；别名场景必须有收敛条件才允许再 sync。

---

## 05 · 记忆管理

### Q5：LTM（会话偏好）和 Knowledge Hub 企业知识为何必须分离？

**【答案】**

记忆四层里，会话摘要/偏好是**个人化、易变、低权限**；KH 发布知识是**团队共识、不可变快照、带门禁**。合并进同一向量库会导致：个人臆测晋升为「企业事实」、ACL 失效、飞轮无法区分反馈对象。

策略：Agent LTM 管「这用户喜欢简洁」；KH 管「这张表字段定义」。产品话术也要分开讲。

**【补充】**

- 对照 interview「驱逐前必先摘要」：上下文压缩是窗口问题；**知识晋升**必须走 staged correction，不是摘要进索引。

### Q5-1：MCP 健康时禁用本地 wiki——是记忆问题还是检索问题？

**【答案】**

是 **真相源唯一性** 问题。本地 wiki 是过时副本（另一种坏记忆）。双源 = 模型注意力被噪声抢占（上下文稀缺）+ 飞轮收不到真实 MCP 流量。默认禁用是信噪比治理。

---

## 06 · 人在回路与持久化执行

### Q6：HITL 解决的本质问题是什么？超时为何不能静默通过？

**【答案】**

本质：运行时智能不确定，**高杠杆决策**（任务拆解、终审、不可逆写）需要人提供设计时未写死的判断；同时执行可能跨小时，必须状态持久化才能跨实例恢复。

超时静默 approve = 假人审，事故不可追责。你方策略把「自动继续」类配置映射为**可审计 auto_reject**（或明确拒绝），符合 fail loud。

**【补充】**

- 对照：中断 vs 确认；定向路由回原会话。  
- KH 侧「人在回路」形态是例外中心/标注任务，不是聊天审批，但同一哲学：不确定项才进人。

### Q6-1：哪些该 HITL，哪些该自动？判断口诀？

**【答案】**

| 自动 | HITL |
|------|------|
| 可收敛、可回滚、置信高的修复（别名映射到白名单表） | 低置信标注、阻断门禁、冲突仲裁、发布策略争议 |
| 确定性转换（convert） | 价值判断（这条知识能否给外包 Agent 看） |

口诀：可逆 + 可验证信号 → 自动；不可逆或信号弱 → 人。

---

## 07 · 流式推送 SSE

### Q7：构建日志 / Agent 流式输出，产品与工程各要注意什么？

**【答案】**

工程：心跳防代理断开；断线恢复靠状态（runId/taskId）；不要假设客户端收齐每一帧。  
产品：进度条隐喻「构建→治理→发布」，降低认知负荷；错误要用业务语言（自动发布被跳过的原因），不是堆栈。

**【补充】**

- 诚实边界：若演示环境代理吞 SSE，要有轮询 fallback——恢复的是状态。

---

## 08 · 安全护栏

### Q8：Agent 护栏为何 ≠ LLM 内容安全？你项目工具层怎么防？

**【答案】**

LLM 护栏管「说什么」；Agent 还管「做什么」。一次注入可触发写工具。纵深：输入 / **工具** / 输出 / 系统。

本项目工具层：

- MCP hardBoundaries：不能直改 published OKF / 随意切 channel。  
- 纠正 staged；admin 门控回点。  
- 工具风险分级 + HITL；`kb_*` 默认偏读。  
- 检索前 ACL（visibility × role），防事后裁剪泄露。  
- HTTP JWT + 限流；stdio 可强制 token。

**【补充】**

- OWASP LLM Top10：injection 第一；间接注入可来自「检索内容」——故 ACL + 发布门禁也是安全。  
- 诚实边界：独立 injection 分类器、输出 Llama Guard 级审核仍为演进。

### Q8-1：为什么检索 ACL 必须在检索前过滤？

**【答案】**

事后裁剪时，敏感片段可能已进上下文或分数分布已泄露存在性。Agent 安全里「看到」即污染。过滤必须在召回路径，且 get_page/get_entity 与 search 共用策略。

---

## 09 · RAG / 知识接地（含「超越纯 RAG」）

### Q9：按幻觉三层根因，对照你的系统各层解法。

**【答案】**

| RAGAS 维 | 故障 | 本项目解法 |
|----------|------|------------|
| Context Recall | 没召回 | 词项+dense RRF；结构化工具（表/图）补盲区；query 侧仍可增强 |
| Context Precision | 召回噪声 | 角色可见性过滤；宁可少给；后续 rerank |
| Faithfulness | 没用材料 | 证据/citation、trust 展示、强制归因、grounding 话术 |
| Answer Relevancy | 跑题 | 意图→工具选择；冲突检测；QA Agent |

另：**版本错误**是第四类「接地失败」——发布冻结 + channel 回点专门治「检对了但是旧世界」。

**【补充】**

- 业界七层流水线你覆盖了检索融合与生成约束；真 embedding、HyDE、cross-encoder、完整 RAGAS CI 为演进。  
- 话术：我们不是反 RAG，是 **Governed Knowledge Plane + Retrieval**。

### Q9-1：为何 hashing dense 而不是一上来 pgvector？关键取舍？

**【答案】**

RRF 需要多路；hashing-trick 无向量库、契约快落地，验证融合与 Agent 兼容。代价是质量上限。取舍符合「先闭环（P0）再检索硬度（P1）」——与产品优先级一致。RRF 接口可保留，底层换真 embedding。

### Q9-2：BM25/词项与向量互补，在策划知识上举例。

**【答案】**

表名、错误码、字段 ID 靠字面命中；「接近的玩法描述」靠语义。单路必有盲区。RRF 用排名融合避开量纲问题（interview 同款论述）。

### Q9-3：Knowledge Hub 相对「切块向量库」多了哪些「语义层」？

**【答案】**

表 schema、图谱关系、立法规则、别名翻译表、证据记录、trust、发布门禁。Agent 可 `kb_get_table_schema` / `kb_get_neighbors`，不是只吞相似段落。这是企业 Agent 差异化，不是更长的 chunk。

---

## 10 · 灾备与成本治理

### Q10：模型与构建成本上，你做了哪些「真金白银」级控制？

**【答案】**

- 构建：增量 vs 全量决策；图谱 scoped 才重跑抽取。  
- 别名：无新增映射不 sync，防死循环烧钱。  
- Agent：熔断、迭代预算、工具循环检测、模型分级（若配置）。  
- 评测 offline 优先，避免回归每次全量打模型。  
- MCP 限流防刷。

灾备：发布可回滚；自动发布可关；健康巡检发现过期。

**【补充】**

- 诚实边界：未做精细到「每工具美元账单」的成本会计，但有防空转与限流。

### Q10-1：生产事故 60 分钟，成本与正确性如何一起考虑？

**【答案】**

先回滚 channel（止损，可能浪费一次错误发布成本但换正确性）→ 再修源；禁止「继续自动发布碰运气」。成本服从正确性，符合 fail loud。

---

## 11 · 多 Agent 与 Workflow

### Q11：拆多 Agent 的依据是「能力」还是「上下文隔离」？你怎么落到策划场景？

**【答案】**

interview 原则：**需要隔离才拆 Agent；需要共享用 Skill/工具**。策划场景里战斗/数值上下文互相污染会导致「各说各话」，且需要不同 prompt/工具——故 Director + 专职子 Agent（Orchestrator 模式）合理。

反模式：为架构图拆 5 个 Agent 再花 80% 时间同步上下文——若最大痛点是共享，说明不该拆。

**【补充】**

- 代价要主动讲：总 token 上升、handoff 误差、调试难；用质量与并行换成本。  
- 冲突检测是「仲裁」机制，不是让 Agent 自己吵完事。

### Q11-1：KH 构建五阶段是 Agent 还是 Workflow？判断标准？

**【答案】**

**设计时智能 vs 运行时智能**：路径已想清（固定五阶段）→ Workflow/流水线；LLM 只在 extract 等节点作执行器。Director 任务图在运行时补足「怎么拆需求」→ Agent。  
口诀：能画稳定执行图 → Workflow；画不出 → Agent。

### Q11-2：SubAgent 回传应带什么？为何不要回传全部轨迹？

**【答案】**

回传蒸馏结论 + 关键引用（release/component）；轨迹留在子上下文。否则 Orchestrator 上下文爆炸（Context Rot），违背「上下文稀缺」。

---

## 12 · AI Coding 基础设施（轻量对照）

### Q12：你如何保证 Agent「基于事实编码/策划」而非臆测？

**【答案】**

对 Coding Agent：代码图谱、测试、类型检查是可验证信号。  
对策划 Agent：KH 发布快照、表工具、证据、冲突检测、强制归因是等价物。  
同一原则：**可验证信号驱动，而非模型自评**。

**【补充】**

- 本地 wiki 双源 = 让 Agent 基于过时事实——已被产品策略关掉。

---

## 13 · Prompt 与 Context 工程

### Q13：上下文工程在你系统里的具体手段？

**【答案】**

Write-Select-Compress-Isolate：

- **Isolate**：子 Agent 分上下文；MCP 工具按需暴露（on_demand + allowlist）。  
- **Select**：结构化工具精取表/实体，少塞长文。  
- **Compress**：回传结论；黑板缓存重复检索。  
- **Write**：纠正/反馈写入 KH，而非塞进无限 system prompt。

Prompt 负责角色与约束；**契约行为**（report/attribution）放 Hook，因 prompt 自觉不可度量。

**【补充】**

- 最小高信号 token：证据 ID 列表往往比整页 wiki 更值钱。

---

## 14 · Harness 工程

### Q14：什么是 Harness？为何说「模型之外的一切决定成败」？

**【答案】**

Harness = 工具、检索、记忆、护栏、评测、编排、观测……模型只是其中一环。同一模型换 Harness，策划 Agent 可从胡编变成可审计。

你的 Harness 关键件：MCP 工具面、发布门禁、飞轮、HITL、熔断、证据 UI、评测 CLI。简历应强调 Harness，而非「调了哪个模型」。

**【补充】**

- Context ⊂ Harness：只优化 prompt 不够。  
- 可退化性：MCP 挂了能否明确失败而非瞎用 wiki——你选择 fail loud + 可选降级开关。

---

## 15 · Plan 与多 Agent 实现范式

### Q15：Plan 硬保障解决什么？和「让模型自己规划」如何共存？

**【答案】**

运行时规划灵活但会越权调工具、死循环。Plan 硬保障：白名单、重规划次数、拒绝未授权工具——把**设计时约束**套在运行时智能上。

共存：模型可提计划；执行期校验计划∩允许工具集。这是 Workflow 约束与 Agent 灵活的折中。

**【补充】**

- 多意图拆分：Director 任务图 = 规划产物；依赖边防止「数值未定先写战斗数值」。

### Q15-1：五种实现范式里你属于哪一种？

**【答案】**

偏 **Orchestrator + 专职 Worker**（中心调度），辅以流水线式知识构建（KH）。不是纯对等辩论群，也不是单 ReAct 包打天下。选型理由：策划领域边界清晰、需要汇总与冲突仲裁。

---

## 16 · 飞轮 / 事件 / 不可变（项目特有深化）

### Q16：事件驱动自动化如何避免「旁路 Service」？多实例怎么办？

**【答案】**

emit 落库 + 广播；订阅者只调 `flywheel.sync` / rebuild 等 Service。  
多实例 EventEmitter 丢/重复 → `outbox + SKIP LOCKED`。  
原则仍是：确定性写路径单一入口。

### Q16-1：别名自动修复如何收敛？请按「可验证信号」讲。

**【答案】**

信号：本轮是否**新增**合法 alias（canonical∈已知表集合）。无新增 → no_action。非法 canonical → 拒绝。这是客观信号，不是 LLM 说「我修好了」。

### Q17：不可变发布的产品-技术双重意义？

**【答案】**

技术：审计、回滚、归因指向稳定 manifest。  
产品：Agent 与人对「当前事实」有共同版本语言；出事能回答「当时用的是哪一版」。

---

# 第二部｜产品岗（同一哲学：本质 → 方案 → 取舍 → 边界）

---

## P-01 定位与非定位

### PQ1：用「问题本质」一句话定义产品，并列出 Non-goals。

**【答案】**

本质：让域内 Agent **基于可发布、可追溯、可回滚的团队知识工作**，并让知识在失败信号下可运营迭代。  

Non-goals：开放域聊天；个人笔记 RAG；任意 Agent 直写共享真相；LTM 与企业知识合并；无门禁「全自动正确」话术。

**【补充】**

- 对照销售话术：不说「企业级通用中台」除非 ACL/评测/网关达标——诚实边界即产品责任。

### PQ1-1：用户是谁？谁不是用户？

**【答案】**

是：策划（生产知识）、运营主策（敢不敢用）、Agent 接入方、管理员。  
不是：终端玩家直接用户。避免 ToC 叙事套错。

---

## P-02 差异化与竞品

### PQ2：相对飞书 AI / 向量知识库 / 自建 LangChain RAG，差异化话术？

**【答案】**

他们强在搜与写；你强在 **发布契约 + 工具契约 + 反馈治理飞轮**。一句话：不是文档更好搜，是 **Agent 可治理地干活**。  
取舍：垂直域先做深，横向连接器后置——避免平台泥潭淹没闭环验证。

---

## P-03 体验与信息架构

### PQ3：「一句话状态 + 一个主动作」解决什么本质问题？

**【答案】**

本质是**注意力预算**：策划上下文装不下五阶段/Lint 域。进度披露分层：默认路径零理解；例外才展开。  
例外中心准入必须严——否则告警疲劳，fail loud 变成 fail noisy。

### PQ3-1：软忽略的产品语义如何讲才不歪？

**【答案】**

忽略 = 确认「此刻不处理」且可审计，≠ 修复。UI 要写清；blocking 应有值班 SLA。

---

## P-04 指标与 Go/No-Go

### PQ4：飞轮「转起来」的可验证指标有哪些？（拒绝虚荣指标）

**【答案】**

| 类型 | 指标 |
|------|------|
| 行为 | MCP 流量占比；自动 report 覆盖率；归因字段完整率 |
| 系统 | 上传→可消费 P95；自动发布跳过 top reason；例外年龄 |
| 质量 | hit@k；事故回滚次数；坏别名回滚次数 |
| 虚荣（少讲） | 工具数量、模型参数量、页面数 |

Go/No-Go：默认 MCP、强制 provenance、门禁尊重 Profile、评测阈值、例外闭环——清单对齐「企业级」期望。

---

## P-05 优先级哲学

### PQ5：为何 P0 先 Agent 闭环而不是先强向量？用第一性原理答。

**【答案】**

没有消费与失败信号，检索优化没有可验证输入（违背「可验证信号驱动」）。先 fail loud 把 miss/坏命中收回来，再提高 Recall/Precision。  
这与 Harness 观一致：先接上工具与回写，再换更好检索后端。

### PQ5-1：资源 1 人月，保什么砍什么？

**【答案】**

保：MCP 默认接通、强制回写、回滚、门禁、例外值班。  
砍：皮肤、工具数量竞赛、自研网关、多模型炫技。  
保的是「敢用、能停、能改」。

---

## P-06 组织与谈判

### PQ6：主策要求关闭自动发布，你怎么谈？

**【答案】**

1. 听恐惧（错版流出 vs 失控感）。  
2. 给档位：关 autoPublish，保留自动构建+Lint；或仅例外清零才自动。  
3. 演示回滚与 reason。  
4. 用积压数据谈全人工成本。  
目标：**可控的自动**，不是赢辩论。

### PQ7：Agent 团队要 bypass KH 直写 wiki？

**【答案】**

默认拒绝。提供够快的 staged 纠正/上传路径，用体验换合规。直写共享真相破坏版本语言与审计——产品事故成本高于短期爽感。

---

## P-07 演示与叙事

### PQ8：15 分钟给非技术领导的演示脚本？

**【答案】**

改表上传→进度条到发布→Agent 提问出示证据→制造缺口进例外→回滚→用 Go/No-Go 讲边界。演示闭环与可停，不演示温度参数。

---

# 第三部｜交叉拷打（技术×产品同一题）

### XQ1：强制归因回写——技术本质 vs 产品本质

**【答案】**

技术：无 release/component/evidence 则不可审计、不可聚类、评测无法回流；prompt 覆盖率不可度量 → Hook 是契约。  
产品：「可信」必须可验证；否则演示成功、事故无法复盘，无法对客户承诺。

**【补充】** 两者都拒绝 fail silent。

### XQ2：自动别名会修错吗？接受条件？

**【答案】**

技术：canonical 白名单、置信阈值、收敛信号、可回滚。  
产品：高置信自动、低置信例外、展示映射可撤销；定期抽检。不接受无审计静默改。

### XQ3：只能加一项能力，选什么？

**【答案】**

优先 **评测门禁进入发布**（检索+忠实度抽样）：同时服务技术回归与产品 Go/No-Go，杠杆高于单加 UI 或单换模型。

---

# 第四部｜模拟套餐（对齐本库风格）

### 套餐 T｜技术 45min

1. Q1 解耦本质（6）  
2. Q9 幻觉三层对照（8）  
3. Q11 Agent vs Workflow + 拆分依据（7）  
4. Q16-1 别名收敛（7）  
5. Q8 工具层安全（6）  
6. Q3 评测分层 + 诚实边界（6）  
7. 反问（5）  

### 套餐 P｜产品 45min

1. PQ1 定位/Non-goals（5）  
2. PQ2 差异化（6）  
3. PQ3 运营台认知负荷（5）  
4. PQ4 指标与 Go/No-Go（8）  
5. PQ5 优先级第一性原理（6）  
6. PQ6 自动发布谈判（8）  
7. PQ8 演示叙事（7）  

### 套餐 X｜综合 60min

T 精简 + P 精简 + XQ1/XQ2。

---

# 第五部｜速记卡（面试前 10 分钟）

| 原则 | 项目落点 |
|------|----------|
| 上下文稀缺 | 分 Agent、工具精取、禁双源 |
| 可验证信号 | 测试真 PG、hit@k、收敛条件、门禁 reason |
| fail loud | 禁止 silent drop；超时不静默批准；发布跳过可见 |
| 恢复状态 | run/task 状态机；channel 回点 |
| 设计时 vs 运行时智能 | 五阶段 Workflow；Director Agent |
| 诚实边界 | hashing dense、评测 CI、细粒度 ACL、OTel 导出 |

| 最强故事（自备 STAR） | 候选 |
|------------------------|------|
| 集成 | toolPrefix / MCP 默认关 |
| 收敛 | 别名死循环防护 |
| 治理 | Profile 未接线→接线 |
| 产品 | 双源关闭与强制回写 |

---

# 第六部｜与 `interview/agent-llm` 主题映射表

| agent-llm | 本题库入口 |
|-----------|------------|
| 01 架构解耦 | Q1–Q1-2 |
| 02 可观测 | Q2–Q2-1 |
| 03 评测 | Q3–Q3-1 |
| 04 工具弹性 | Q4–Q4-1 |
| 05 记忆 | Q5–Q5-1 |
| 06 HITL/持久化 | Q6–Q6-1 |
| 07 SSE | Q7 |
| 08 安全 | Q8–Q8-1 |
| 09 RAG | Q9–Q9-3 |
| 10 成本灾备 | Q10–Q10-1 |
| 11 多 Agent | Q11–Q11-2 |
| 12 AI Coding | Q12 |
| 13 Prompt/Context | Q13 |
| 14 Harness | Q14 |
| 15 Plan 范式 | Q15–Q15-1 |
| （扩展）飞轮不可变 | Q16–Q17 |
| 产品 | PQ1–PQ8 |
| 交叉 | XQ1–XQ3 |

---

*生成说明：题库按 `interview` 的问答哲学与 01–15 覆盖面重写，案例绑定 Knowledge Hub / design-agent-ts；请按个人真实贡献改「我做了/我主导了」口径。*
